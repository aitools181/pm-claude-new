# Version history

Convention: **MAJOR.MINOR** — bump MAJOR for a big update (new capability area,
breaking change, major release), MINOR for a small change (fix, tweak, small addition).
The running version is shown only in the platform console (`/superadmin`), next to the title.

To change it, edit `packages/shared/src/version.ts` (`APP_VERSION`) and the root `package.json`.

| Version | Date | Summary |
|---------|------|---------|
| 7.6 | 2026-08-15 | Web build fix — extended the deploy-equivalent typecheck to apps/web (real tsc with Next/React/Radix/socket.io types installed), which caught two genuine `next build` blockers the parser-only checker could not: (1) the board assignee popover's Unassign row used `<Icon name=\"none\">` — \"none\" is not a member of the IconName union — now `close`; (2) the project list section menu's Rename row used `<Icon name=\"name\">` — now `tag`. With those fixed and all third-party type packages present, the full web tree (app + components + lib) typechecks at ZERO errors, and the worker, api, db and shared packages were re-verified at zero real errors each (the worker's earlier implicit-any flags were confirmed to be artifacts of missing bullmq types in the scratch environment — they vanish once bullmq is installed, as it is in the container build). All source gates green: production-readiness 42/42, css-bindings PASS, screenshot-parity 25/25, f29-f42 65/65. |
| 7.5 | 2026-08-15 | Deploy build fix — real cross-file typecheck of @pm/shared, @pm/db and @pm/api (the exact steps the Coolify Docker build runs), not just per-file parsing. Fixed four issues the parser-only checker could not catch: (1) packages/db/src/schema/platform.ts referenced `organizations` without importing it — the error that failed the deploy (TS2304); now imported from ./identity.js. (2) Storage metering (item 16) queried a non-existent `schema.files` table — corrected to `schema.attachmentVersions` (the table that actually carries the `bytes` column) in both the superadmin metrics service and the workspace storage endpoint. (3) The deactivation-wizard owned-summary (item 4) used `schema.forms.createdBy`/`deletedAt` and `schema.documents.createdBy`/`deletedAt`, which those tables do not have — corrected to `forms.createdByUserId` (no soft-delete) and `documents.ownerUserId` (no soft-delete). (4) The role-change session invalidation (item 20) failed to inject SessionService because the constructor was multi-line and the earlier edit's pattern did not match — the optional SessionService dependency is now correctly declared. All four workspace builds now typecheck clean (0 real errors; only expected third-party module-resolution notes that resolve at container build time). All source gates remain green: production-readiness 42/42, css-bindings PASS, screenshot-parity 25/25, f29-f42 65/65. |
| 7.4 | 2026-08-15 | Blueprint gap batch 5 - FINAL: catalogue, idempotency, guests, session hygiene, meeting capture (review items 17, 18, 19, 20, 21). Item 17 (9.16): the WORK_ITEM_* validation codes already thrown across the API are now a formal contract in packages/shared/src/work-item-errors.ts (18 stable codes with canonical descriptions), and a new production-readiness gate cross-checks that every thrown WORK_ITEM_* code is declared in the catalogue (42 checks total now). Item 18 (9.17): the UI-path create endpoints (POST /work-items and POST /work-items/:id/subtasks) now run through the same reservation-based IdempotencyInterceptor as the public API; the web api() helper accepts an idempotencyKey option and the global Create dialog generates a fresh key per open, so a double-click or network retry can never create two tasks. Item 19 (F03): organization_memberships gain account_type (member|guest, migration 0038, journal idx 38) - invitation accept with the guest role records a guest account, the directory returns it, and Admin > People shows a Guest badge; role capabilities keep governing what guests can do. Item 20 (F02): sensitive-session invalidation - assigning or unassigning a role now revokes every session of the affected user (self-changes exempt, fail-soft), so permission changes only take effect through re-authentication and stale sessions cannot retain old capabilities; wired via the globally exported SessionService. Item 21 (F40): meetings gain a transcript column and capture pipeline - POST /meetings/:id/transcript stores pasted capture text (500k cap), POST /meetings/:id/extract-actions runs a heuristic extractor (Action:/todo:/'X will …'/follow-up lines, deduped, 30 cap - unit-smoked) and the meetings page gains a Transcript panel with Extract action items and one-click Add as action, feeding the existing action -> work-item convert flow. API surface: 652 routes / 574 mapped calls; all gates green. |
| 7.3 | 2026-08-15 | Blueprint gap batch 4 - auth depth, formula fields, observability, storage metering (review items 7, 10, 15, 16). Item 7 (F02): users gain a unique optional username (migration 0037, journal idx 37) - login accepts email or username (identifier without @ resolves by handle; the login page label, input type and autocomplete updated; Settings > Profile gains a username field with server-side uniqueness); organization_settings gains password_policy jsonb - Settings > Workspace exposes min length plus A-Z/0-9/symbol toggles, enforced on invitation accept (inviting org's policy) and password reset (strictest across the user's orgs, floor 10 chars); suspicious-login alerts - a successful sign-in from an IP the account has never used before (and not the first session ever) appends an instance-scope auth.suspicious_login audit event and emails the user, checked before the new session row exists and failing soft so mail outages never block login. Item 10 (F11/F32): formula custom-field type with a dependency-free, injection-safe arithmetic evaluator (numbers, identifiers, + - * /, parentheses; unknown vars read 0, division by zero yields 0, malformed or non-finite input returns null - unit-smoked incl. require() injection); valuesForItem computes formulas from item intrinsics (story_points, estimate_minutes, estimate_hours, progress) plus sibling numeric fields; the field builder gains the formula type with an expression input and the task pane renders computed values read-only with an f badge. Item 15 (NFR 8.4): GET /superadmin/metrics returns process uptime, memory, live DB latency and platform totals as JSON or Prometheus text (?format=prometheus). Item 16 (F01): storage metering - GET /superadmin/organizations/:id/storage and org-level GET /workspace/storage-usage sum file bytes/counts; the workspace plan card now shows MB and file count. API surface: 650 routes / 571 mapped calls; all gates green. |
| 7.2 | 2026-08-15 | Blueprint gap batch 3 - planning & collaboration depth (review items 9, 11, 12). Item 9, skills registry (F16): new user_skills table (migration 0036, journal idx 36) with org-unique (user, skill) and level 1-5; endpoints GET/PUT /me/skills (self-service), GET /skills/matrix and PUT /users/:id/skills (RESOURCE_MANAGE) and GET /skills/suggest?skill= which ranks members by highest level then lightest open workload - the two F16 suggestion signals; Admin > People gains a Skills tab with the org matrix, inline `skill:level` editing and a suggest-assignees search. Item 11, approval gate in workflows (F12/F18): new approval_required validator kind in the workflow rules engine - a transition only succeeds when an approval request on the work item is fully approved (config.definitionId can pin one definition; rejected blocks with a precise reason, pending explains itself); the workflow builder gains a per-transition + gate control offering approval/comment/field validators and assign-actor/set-progress post-actions, using the existing rules endpoint that previously had no UI. Item 12, notification digest and quiet hours (F23): new notification_delivery_settings and notification_digest_queue tables; the email channel now actually delivers - notify() honours the per-type email preference, then routes through quiet hours (deferred, sent individually when the window ends) or a daily/weekly digest (one combined email at the chosen org-local hour, weekly on Monday), otherwise sends immediately; a self-contained 10-minute in-process flusher does the draining (no external worker), org timezone aware with midnight-wrapping quiet windows; GET/PUT /notifications/delivery-settings plus a new Email delivery section in Settings > Notifications. API surface: 647 routes / 570 mapped calls; all gates green. |
| 7.1 | 2026-08-14 | Blueprint gap batch 2 - security NFRs + governance (review items 13, 14, 5, 6). Item 13, API-wide rate limiting (NFR 8.1): dependency-free in-memory sliding-window middleware in the API bootstrap - 300 req/min general and 30 req/min for auth endpoints per session (falling back to client IP), env-tunable via RATE_LIMIT_PER_MINUTE / RATE_LIMIT_AUTH_PER_MINUTE, health endpoints exempt, 429 responses carry Retry-After and X-RateLimit-* headers; the login lockout stays separate. Item 14, security headers: the API now sets a deny-all CSP, nosniff, DENY framing, no-referrer, Permissions-Policy, COOP/CORP and HSTS-when-HTTPS on every response; apps/web/next.config.mjs gains a headers() block with a same-origin CSP (inline styles and ws upgrades allowed), nosniff, frame denial and referrer policy. Item 5, super-admin support access (F01): new support_access_grants table (migration 0035, journal idx 35) - a platform admin must start a time-bound (15 min to 8 h) grant with a mandatory reason before entering an organization they are not a member of; grant start/end are instance-scope audit events; the org-context gate honours only active grants (including into suspended orgs, which stay closed to members) and the org switcher lists support-access orgs with a marker; the superadmin console gains a Support access tab (grant table, live state, End now) and a per-org Support access action. Item 6, export-before-delete (F01): archiving an organization now requires a completed export in the last 7 days - `POST /superadmin/organizations/:id/export` streams the full data-ops bundle as a download and records platform.organization_exported; the status endpoint rejects archive with code export_required otherwise; the console gains per-org Export and Archive actions with the guided error. API surface: 640 routes / 563 mapped calls; all gates green. |
| 7.0 | 2026-08-14 | Blueprint gap batch 1 - People & Organization foundation (review items 1, 2, 3, 4, 8). Migration `0034_people_org_foundation.sql` (journal idx 34). Item 1, extended user profile (F03): `users` gains avatar_url, designation, department, manager_user_id, working_hours jsonb and contact_fields jsonb; `PATCH /me/profile` accepts them all with self-manager and org-membership validation; `/directory/members` returns the new fields; Settings > Profile gets photo URL with avatar preview, designation/department, manager dropdown, phone/mobile/location and working-hours inputs. Item 2, bulk invitation + CSV user import (F03): `POST /invitations/bulk` dedupes within the batch and against members/pending invites with a per-email report; `POST /invitations/import-csv` parses email,displayName,roleKey rows (header optional, 500-row cap), validates roles against the org catalogue, classifies every row (invited / already member / already invited / duplicate in file / unknown role / invalid email), writes an audit event and returns a failed-rows CSV for correct-and-reimport; Admin > People gains both panels plus an inline import-report table with download. Item 3, team depth (F03): teams gain leader_user_id, parent_team_id (cycle-safe re-parenting) and description; team_members gain effective_from/effective_to; full CRUD plus membership endpoints; the Teams tab placeholder becomes real management UI with a team detail panel. Item 4, deactivation reassignment wizard (F03): `GET /directory/members/:id/owned-summary` counts owned projects/tasks/forms/automations/documents; `POST /directory/members/:id/deactivate` transfers project and work-item ownership in one transaction, suspends the membership and appends a reasoned audit event; Admin > People gets the two-step wizard modal on every member row using the shared dialog behavior. Item 8, organization settings depth (F01): organization_settings gains time_format, number_format (Indian grouping included), working_days jsonb, fiscal_year_start_month and retention_days; the workspace-settings endpoint and Settings > Workspace expose them with a working-days day-toggle row. API surface grew to 636 routes / 558 mapped frontend calls; all gates green. |
| 6.9 | 2026-08-14 | CSS-binding audit and permanent guard. Full sweep of apps/web: 1142 classNames used across 97 route pages and all components were diffed against the 1374 selectors in the four CSS layers. Result: zero unbound classes remain - the only unmatched tokens are dynamic families that resolve at runtime (`bg-*`, `h-*`, `health-*`, `priority-*`, `st-*`, `project-health-*`) and marker classes that carry only RuntimeStyle CSS variables (`runtime-*`, `col-*`), all verified to resolve to defined full forms. Page-wrapper pass confirmed every route lands on a styled container (the 14 advanced-module pages share the styled AdvancedModuleHub, two routes are redirects, and the three regex flags were early loading guards, not real wrappers). Added `scripts/verify-css-bindings.cjs` - a dependency-free gate that fails the build whenever a className is used in TSX without a matching CSS selector - and wired it into the README fast-verification list, so markup can never again ship without its layout layer. |
| 6.8 | 2026-08-14 | Layout repair + platform-admin full access, driven by live deployment screenshots. Root cause of the broken UI: several functional surfaces had working markup and API wiring but no layout CSS, so they rendered in default inline flow - My tasks list rows (`.asana-task-row` grid now mirrors the sticky head), My tasks board (`.board-column`/`.board-cards`/`.task-card` suite), the month calendar (7-column `.month-grid` with hover day-add), the project list (toolbar row, sticky header grid, row grid driven by a runtime `--list-cols` variable so added/hidden fields stay aligned, name-cell indent by `--task-depth`, add-row, add-section, column-add menu), shared view toolbars, the Browse projects table (grid corrected from 5 declared columns to the 7 actually rendered: name, members, team, portfolios, status, last modified, actions), the My tasks files grid/list, and project-header polish (member faces, favorite star, status dot, icon tile). All new rules are token-only. Platform admin full access: `PlansService` now takes an optional `PlatformAdminService`; `entitlements`, `assertWithinLimit` and `assertModuleAllowed` accept the acting userId and bypass plan gating for platform admins - `GET /billing/entitlements` returns a `platform_admin` plan (unlimited limits, all optional modules), project-creation limits and module toggles no longer block platform admins, and the sidebar plan card shows "Platform admin - Full access" with no billing CTA instead of "Free". |
| 6.7 | 2026-08-14 | Asana-parity batch 3 (screenshot items 11-19; item 20, per-row starring in the projects directory, already existed). New project Workflow view tab: stage flow (To do -> In progress -> Done) with live counts and task peek from the board endpoint, plus an automation panel reading `/automation/rules` with a graceful message when the caller lacks the permission. Home People widget shows per-member overdue/completed/upcoming counts from the new aggregate `GET /directory/overview` (counts only - no titles leak past project access). The Home hero "My week" chip is now a dropdown (My week / My month) persisted in navigationPreferences; the tasks-completed chip counts within the chosen window. My tasks Calendar gains an Asana-style Weeks strip (7 day columns, prev/next week, per-day Add task) with a Weeks/Month toggle. Timeline gains a Weeks/Months/Quarters zoom preset plus labelled zoom in/out. The task pane visibility line becomes the full-width privacy banner with Make public / Make private inline. The sidebar groups projects under collapsible team (workspace) sections with a per-team + that opens the Create project dialog, and every project row gains a color-and-icon popover (hover ... or right-click) that PATCHes the project. The topbar sparkles button now opens an in-context AI assistant panel: permission-aware answers via `/search`, provider state via `/ai/status`, result cards linking to projects/tasks/comments, and "Turn into task" which opens the global Create dialog prefilled (Create dialog accepts initialTitle; the pm:open-create event carries it). |
| 6.6 | 2026-08-14 | Asana-parity batch 2 (screenshot items 6-10). Project title menu becomes the full Actions menu: Duplicate project (new `POST /projects/:id/duplicate` copies metadata, sections, tasks and one level of subtasks with a fresh org-unique key prefix; comments/attachments intentionally excluded), Save as template (snapshots sections+items into `POST /templates` kind=project), Add to portfolio submenu (`POST /portfolios/:id/projects`), Archive/Unarchive (status patch) and Delete project (confirm + soft delete + redirect). Project Dashboard gains an editable chart system: charts persist per project in a `viewType: dashboard` saved view, each chart has a View modal, an Edit form (title, bars/donut, group by section/assignee/priority/status, status+due filters with an Asana-style filter chip) and Add chart/Remove chart. Board cards gain a subtask expand pill (inline subtask list, opens in the task pane) and an assignee popover that PATCHes the owner directly from the card. The sidebar footer gains a live plan widget driven by `GET /billing/entitlements` (trial/past-due states, Add billing info CTA) plus a persistent Invite button that opens the global Invite dialog via a `pm:open-create` event. The task pane expand control now cycles normal, wide, full screen. |
| 6.5 | 2026-08-14 | Asana-parity batch 1 (screenshot items 1-5). Global Create button now opens the full create menu - Task, Project, Page, Message, Team, Portfolio, Goal, AI Teammate, Invite - each wired to its real endpoint (`/projects`, `/documents`, `/projects/:id/messages`, `/workspaces`, `/portfolios`, `/goals`, `/invitations`). Home gains a "Tasks I've assigned" widget backed by the new `GET /my-work/delegated` route (reporter = me, owner someone else, access-filtered, owner names joined). My tasks gains a Dashboard tab (KPIs, tasks by project, completion donut, open-by-priority). Project list column headers open per-column menus (sort ascending/descending on Name/Due date/Priority, Hide field on any column) with a new sort-direction state. Section headers gain a More actions menu: Add task, Rename section (PATCH), Move tasks to another section (PATCH per item), Delete section (DELETE; server already un-sections its tasks). Also repaired the stale production-readiness logout check to follow the shared `lib/logout.ts` helper introduced in 6.0, so the gate reports 41/41. |
| 6.4 | 2026-08-14 | Works on Coolify 4.x, where variables generated from the compose file are read-only. Coolify seeds each `${VAR}` with the compose default - including `:?` guard messages, so `APP_URL` became `http://localhost:3000` and `POSTGRES_PASSWORD` became the literal string `Set POSTGRES_PASSWORD in .env` - and then locks the entry. The API now also accepts the domain Coolify assigns to the web service (`SERVICE_URL_WEB` and friends) as an allowed origin, accepts a bare hostname, ignores values that still read like a guard message, and warns at boot if `SESSION_SECRET` is still a placeholder. `docker-compose.yml` documents the single line to edit if the automatic route does not resolve. |
| 6.3 | 2026-08-14 | CORS no longer rejects logins over a configuration typo. Allowed origins were compared as exact strings, so a trailing slash or a path in `APP_URL` failed to match the browser's `Origin` header. Origins are now canonicalised to scheme + host + port on both sides, the rejection message names the offending origin and the allow-list, and the effective allow-list is logged at boot. |
| 6.2 | 2026-08-13 | The web container was proxying `/api` and `/socket.io` to itself. Next evaluates `rewrites()` during `next build` and bakes the destination into `routes-manifest.json`; `INTERNAL_API_URL` was set only on the running container, so the destination baked as `http://localhost:4000` and every API call came back as a plain-text 500 - the "Request failed" seen on login, and silently broken realtime. `INTERNAL_API_URL` is now a build arg, and the image build fails outright if the destination ever bakes to localhost again. |
| 6.1 | 2026-08-13 | Errors say what actually went wrong. `AppErrorFilter` was `@Catch(AppError)`, so any other exception fell through to Nest's default filter and returned `{statusCode, message}` - a shape the web client does not read, which is why a failed login showed only "Request failed". The filter now catches everything, keeps one response shape, logs the real stack server-side, and returns a short summary. The client also appends the HTTP status when no message is supplied. |
| 6.0 | 2026-08-13 | Sign in works again, and sign out is always reachable. Two independent defects blocked login: form buttons defaulted to `type="button"` so clicking never submitted, and the submit button was disabled whenever React state for email/password was empty - which browser autofill leaves empty, so pressing Enter did nothing either. Auth forms now read values from the form itself, validate on submit with a visible message, and only disable while a request is in flight. Adds a dedicated `/logout` route and a Log out entry on the product rail, alongside the account menu, sidebar footer and Settings -> Account. Also adds error boundaries (`app/error.tsx`, `app/(app)/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx`) so a render failure shows a readable message with a retry instead of a blank white page, and a browser regression spec at `apps/web/e2e/auth-signin.spec.ts` covering all of the above against a stubbed API. |
| 5.9 | 2026-08-13 | Sign in (and every other form button) works again. `Button` defaults to `type="button"`, so the primary buttons inside `<form onSubmit=...>` never submitted — clicking "Sign in" did nothing at all, with no error, because the handler never ran. Only Enter in a field worked. Every in-form submit button now sets `type="submit"` explicitly: login, password recovery, password reset, invitation accept, project/board task create, add section, subtask and checklist add. |
| 5.8 | 2026-08-13 | API boot diagnostics. A failed database migration now prints the reason and a targeted hint instead of disappearing behind the orchestrator's generic "container is unhealthy". Documents the Postgres behaviour where `POSTGRES_PASSWORD` is applied only when the data volume is first created, which is what made the API exit ~5s after start. |
| 5.7 | 2026-08-13 | Production build fix and sign-out recovery. `/settings/account` now wraps `useSearchParams()` in a Suspense boundary and the whole authenticated `(app)` route group renders dynamically, so Next.js no longer fails prerendering one page at a time during the Coolify build. Log out is reachable from the account menu, the sidebar footer and Settings → Account, and all three share one helper that revokes the session, drops the socket and clears local cookies. |
| 5.2 | 2026-08-07 | Container health checks for api, web and worker, so the reverse proxy only routes traffic once a container is actually ready. Adds `/healthz` on web and a health listener on the worker. |
| 5.1 | 2026-08-07 | SMTP email delivery: instance mail settings in the platform console, password encrypted at rest, connection test with recorded result, and real delivery for invitations, password resets and verification (log adapter fallback). |
| 5.0 | 2026-08-07 | Platform console (instance admin, org suspension, module entitlements, platform flags, audit) and Plans & pricing (catalogue, editable prices, limits, plan-gated modules, public pricing page). Includes build fixes and core-flow repairs (default workspace on setup, workspace creation UI, multi-organization). |
