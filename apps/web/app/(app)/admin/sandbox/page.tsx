"use client";
import { AdvancedModuleHub } from "../../../../components/advanced/AdvancedModuleHub";
export default function SandboxPage() { return <AdvancedModuleHub title="Administration sandbox" eyebrow="F37 · Configuration promotion" description="Test configuration in an isolated organisation boundary, inspect signed diffs and promote with approval and rollback evidence." icon="settings" overviewEndpoint="/sandbox" moduleName="sandbox" features={["Configuration-only or masked-sample sandbox creation", "Versioned workflow, field, type, role, automation and form packages", "Source-target diff, dependency graph and compatibility checks", "Test execution, reviewer approval and scheduled promotion", "Restricted integrations, email suppression and synthetic users", "Signed checksums, immutable audit and rollback package"]} actions={[{ label: "Create sandbox", endpoint: "/sandbox/environments", description: "Create an isolated environment; production secrets are never copied by default.", fields: [{ key: "name", label: "Sandbox name", required: true, placeholder: "Release validation" }, { key: "mode", label: "Clone mode", kind: "select", options: [{ label: "Configuration only", value: "configuration_only" }, { label: "Masked sample data", value: "masked_sample" }] }, { key: "label", label: "Environment label", placeholder: "SANDBOX" }] }, { label: "Build configuration package", endpoint: "/sandbox/packages", description: "Capture configuration as a signed, versioned promotion package.", fields: [{ key: "name", label: "Package name", required: true }, { key: "description", label: "Change summary", kind: "textarea" }, { key: "sandboxId", label: "Sandbox ID" }] }]} />; }
