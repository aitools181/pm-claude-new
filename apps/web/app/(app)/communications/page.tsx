"use client";
import { AdvancedModuleHub } from "../../../components/advanced/AdvancedModuleHub";
export default function CommunicationsPage() { return <AdvancedModuleHub title="Connected communications" eyebrow="F40 · Email, calendar and meeting capture" description="Keep task context while capturing inbound email, calendar sync, clips and reviewed meeting actions." icon="meeting" overviewEndpoint="/communications" moduleName="communications" features={["Project/shared inbound mailboxes and thread correlation", "Reply templates, signatures, attachments and delivery audit", "Google, Microsoft and CalDAV two-way calendar sync", "Conflict-safe sync tokens with no hidden date overwrite", "Consent-controlled screen/audio clips and searchable transcripts", "AI meeting summaries and human-reviewed task proposals"]} actions={[{ label: "Create inbound mailbox", endpoint: "/communications/mailboxes", description: "Route authenticated inbound email into a project or shared workspace.", fields: [{ key: "projectId", label: "Project", kind: "project" }, { key: "address", label: "Mailbox address", required: true, placeholder: "support@example.com" }, { key: "name", label: "Mailbox name", required: true }, { key: "routingRules", label: "Routing rules (JSON)", kind: "json", placeholder: "{}" }] }, { label: "Connect calendar", endpoint: "/communications/calendar/connections", description: "Register a calendar connection; credentials remain in the linked integration vault.", fields: [{ key: "provider", label: "Provider", kind: "select", required: true, options: [{ label: "Google Calendar", value: "google" }, { label: "Microsoft 365", value: "microsoft" }, { label: "CalDAV", value: "caldav" }] }, { key: "calendarExternalId", label: "Calendar ID", required: true }] }]} />; }
