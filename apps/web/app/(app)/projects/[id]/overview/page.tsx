"use client";
import { appPrompt } from "../../../../../components/ui/AppDialog";
import { useEffect,useMemo,useState } from "react";import {useParams} from "next/navigation";import {api} from "../../../../../lib/api";import {ProjectChrome} from "../../../../../components/project/ProjectChrome";import {TaskDrawer} from "../../../../../components/work/TaskDrawer";import {Icon} from "../../../../../components/ui/Icon";
type Project={id:string;name:string;keyPrefix:string;color?:string;health:string;status:string;privacy:string;version:number;description?:string|null;startDate?:string|null;dueDate?:string|null;ownerUserId?:string};type Item={id:string;key:string;title:string;typeKey?:string;statusCategory:string;dueDate:string|null;updatedAt:string};type Member={id:string;userId:string;displayName:string;email:string;accessLevel:string};type Update={id:string;health:string;title?:string;headline?:string;body:string|null;createdAt:string};type Resource={id:string;kind:string;name:string;url:string|null;body:string|null};
export default function Overview(){const id=useParams().id as string;const [project,setProject]=useState<Project|null>(null);const [items,setItems]=useState<Item[]>([]);const [members,setMembers]=useState<Member[]>([]);const [updates,setUpdates]=useState<Update[]>([]);const [resources,setResources]=useState<Resource[]>([]);const [openId,setOpenId]=useState<string|null>(null);async function load(){const [p,i,m,u,r]=await Promise.all([api<Project>(`/projects/${id}`,{org:true}),api<Item[]>(`/projects/${id}/work-items?limit=500`,{org:true}).catch(()=>[]),api<Member[]>(`/projects/${id}/members`,{org:true}).catch(()=>[]),api<Update[]>(`/projects/${id}/status-updates`,{org:true}).catch(()=>[]),api<Resource[]>(`/projects/${id}/resources`,{org:true}).catch(()=>[])]);setProject(p);setItems(i);setMembers(m);setUpdates(u);setResources(r)}useEffect(()=>{load().catch(()=>{})},[id]);const milestones=useMemo(()=>items.filter(x=>x.typeKey==='milestone').slice(0,8),[items]);async function addResource(){const name=await appPrompt('Resource name');if(!name)return;const url=await appPrompt('URL (optional)')||undefined;await api(`/projects/${id}/resources`,{method:'POST',org:true,body:JSON.stringify({name,kind:'link',url})});load()}
if(!project)return <div className="asana-loading-page">Loading project…</div>;return <><ProjectChrome project={project} view="overview" onProjectChange={load}/><div className="overview-grid"><main className="overview-main"><section className="overview-ai-card"><div className="overview-section-title"><span><Icon name="sparkles" size={18}/>Project summary</span><a href="/ai">Open AI assistant</a></div><p>{project.description||`Track ${items.length} work items, recent decisions, risks, and milestones for ${project.name}.`}</p><div className="overview-ai-stats"><span><strong>{items.filter(x=>x.statusCategory==='done').length}</strong> completed</span><span><strong>{items.filter(x=>x.statusCategory==='in_progress').length}</strong> in progress</span><span><strong>{items.filter(x=>x.dueDate&&new Date(x.dueDate)<new Date()&&x.statusCategory!=='done').length}</strong> overdue</span></div></section><section className="overview-card"><div className="overview-section-title"><span>Project description</span><button className="text-button" onClick={()=>location.reload()}>Refresh</button></div><p className="overview-description">{project.description||'Add a clear project brief, goals, scope, and working agreement from Customize.'}</p></section><section className="overview-card"><div className="overview-section-title"><span>Project roles</span><button className="text-button" onClick={()=>document.querySelector<HTMLButtonElement>('.project-head-actions .btn')?.click()}>Manage members</button></div><div className="role-grid">{members.slice(0,8).map(m=><div key={m.id}><span className="mini-avatar">{m.displayName.slice(0,1)}</span><span><strong>{m.displayName}</strong><small>{m.accessLevel.replace('_',' ')}</small></span></div>)}</div></section><section className="overview-card"><div className="overview-section-title"><span>Key resources</span><button className="text-button" onClick={addResource}><Icon name="plus" size={14}/>Add resource</button></div>{resources.length===0?<div className="overview-empty">Pin briefs, docs, dashboards, and important links here.</div>:<div className="resource-list">{resources.map(r=><a key={r.id} href={r.url||'#'} target={r.url?'_blank':undefined}><Icon name={r.kind==='file'?'paperclip':'link'} size={16}/><span><strong>{r.name}</strong><small>{r.body||r.url||r.kind}</small></span></a>)}</div>}</section><section className="overview-card"><div className="overview-section-title"><span>Milestones</span><a href={`/projects/${id}/timeline`}>View timeline</a></div>{milestones.length===0?<div className="overview-empty">Add a milestone from the Add task menu.</div>:milestones.map(x=><button className="milestone-row" key={x.id} onClick={()=>setOpenId(x.id)}><span className="milestone-diamond">◆</span><span>{x.title}</span><time>{x.dueDate||'No due date'}</time></button>)}</section></main><aside className="overview-side"><section className="overview-side-card"><h3>Project details</h3><dl><div><dt>Status</dt><dd>{project.status.replace('_',' ')}</dd></div><div><dt>Health</dt><dd>{project.health.replace('_',' ')}</dd></div><div><dt>Dates</dt><dd>{project.startDate||'—'} → {project.dueDate||'—'}</dd></div><div><dt>Members</dt><dd>{members.length}</dd></div><div><dt>Privacy</dt><dd>{project.privacy}</dd></div></dl></section><section className="overview-side-card"><h3>Recent status</h3>{updates.slice(0,4).map(u=><article className="status-update-mini" key={u.id}><span data-health={u.health}/><div><strong>{u.headline||u.title}</strong><small>{new Date(u.createdAt).toLocaleDateString()}</small></div></article>)}{!updates.length&&<p className="muted">No status updates yet.</p>}</section><section className="overview-side-card"><h3>Connected work</h3><a className="overview-connect" href="/goals"><Icon name="goal" size={16}/>Goals</a><a className="overview-connect" href="/portfolios"><Icon name="portfolio" size={16}/>Portfolios</a><a className="overview-connect" href="/devops"><Icon name="release" size={16}/>Development links</a></section></aside></div>{openId&&<TaskDrawer id={openId} onClose={()=>setOpenId(null)} onSaved={load}/>}</>}
