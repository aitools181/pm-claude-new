import type { ReactNode } from "react";
export function Sidebar({ items, current, header, footer, collapsed = false, ariaLabel = "Sidebar", className = "" }: { items: Array<{ id: string; label: ReactNode; href: string; icon?: ReactNode }>; current?: string; header?: ReactNode; footer?: ReactNode; collapsed?: boolean; ariaLabel?: string; className?: string }) { return <aside className={`ui-sidebar ${className}`.trim()} data-collapsed={collapsed || undefined}><nav aria-label={ariaLabel}>{header ? <div className="ui-sidebar-header">{header}</div> : null}<div className="ui-sidebar-items">{items.map((item) => <a key={item.id} href={item.href} aria-current={current === item.id ? "page" : undefined} title={collapsed ? String(item.label) : undefined}>{item.icon ? <span className="ui-sidebar-icon">{item.icon}</span> : null}<span className="ui-sidebar-label">{item.label}</span></a>)}</div>{footer ? <div className="ui-sidebar-footer">{footer}</div> : null}</nav></aside>; }
