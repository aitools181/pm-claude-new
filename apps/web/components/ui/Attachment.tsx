import type { ReactNode } from "react";
import { Icon } from "./Icon";
import { Progress } from "./Display";
import { IconButton } from "./Button";
export function Attachment({ name, meta, progress, status = "ready", onRetry, onRemove, actions, className = "" }: { name: string; meta?: ReactNode; progress?: number; status?: "uploading" | "ready" | "error"; onRetry?: () => void; onRemove?: () => void; actions?: ReactNode; className?: string }) { return <div className={`ui-attachment ${className}`.trim()} data-status={status}><span className="ui-attachment-icon"><Icon name="paperclip" size={20} /></span><span className="ui-attachment-copy"><strong>{name}</strong>{meta ? <small>{meta}</small> : null}{status === "uploading" && progress !== undefined ? <Progress value={progress} label={`Uploading ${name}`} /> : null}{status === "error" ? <small role="alert">Upload failed. Retry or remove the file.</small> : null}</span><span className="ui-attachment-actions">{status === "error" && onRetry ? <button type="button" className="ui-link-button" onClick={onRetry}>Retry</button> : null}{actions}{onRemove ? <IconButton label={`Remove ${name}`} icon="close" onClick={onRemove} /> : null}</span></div>; }
