import type { ReactNode } from "react";
import { Avatar } from "./Composite";
export function Bubble({ children, mine = false, className = "" }: { children: ReactNode; mine?: boolean; className?: string }) { return <div className={`ui-bubble ${className}`.trim()} data-mine={mine || undefined}>{children}</div>; }
export function Message({ author, avatar, timestamp, children, actions, mine = false, className = "" }: { author: string; avatar?: string; timestamp?: ReactNode; children: ReactNode; actions?: ReactNode; mine?: boolean; className?: string }) { return <article className={`ui-message ${className}`.trim()} data-mine={mine || undefined}><Avatar name={author} src={avatar} size="small" /><div><header><strong>{author}</strong>{timestamp ? <time>{timestamp}</time> : null}{actions ? <span>{actions}</span> : null}</header><Bubble mine={mine}>{children}</Bubble></div></article>; }
export function MessageScroller({ children, ariaLabel = "Messages", className = "" }: { children: ReactNode; ariaLabel?: string; className?: string }) { return <div className={`ui-message-scroller ${className}`.trim()} role="log" aria-label={ariaLabel} aria-live="polite" aria-relevant="additions">{children}</div>; }
